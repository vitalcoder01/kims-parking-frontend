// Keep these two in sync with android/app/build.gradle's versionCode/versionName
// on every release — this is what the app compares against the backend's
// /api/app/version response to decide whether to show the update prompt.
export const APP_VERSION_CODE = 51;
export const APP_VERSION_NAME = '1.9.17';
