<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plaintext HTTP only to the local dev backend (adb reverse tunnels
  localhost:4000 to the PC, and the LAN IP is a fallback over WiFi). All
  other traffic still requires HTTPS (base-config below).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.1.5</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
